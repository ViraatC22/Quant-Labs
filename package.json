{
  "name": "quant-labs",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "web:dev": "npm --prefix apps/web run dev",
    "web:lint": "npm --prefix apps/web run lint"
  }
}
